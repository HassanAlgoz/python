# Pair Peers: given a list of tuples representing (name, age) of each person.
# Pair people such that the age difference is minimized.
def pair_peers(people: list[tuple[str, int]]) -> list[tuple[str, str]]:
    pass

# Distribute Students:
# Case 1: number of groups is specified
# Case 2: size of a group is specified
def distribute_students(students: list[str], num_groups: int = 0, group_size: int = 0) -> list[list[str]]:
    pass